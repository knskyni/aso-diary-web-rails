<!DOCTYPE HTML>
<html lang="ja">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>Starter Template · Bootstrap</title>

    <meta name="description" content="">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- stylesheet -->
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <!-- javascript -->
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <style>
        .bd-placeholder-img {
            font-size: 1.125rem;
            text-anchor: middle;
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }

        @media (min-width: 768px) {
            .bd-placeholder-img-lg {
                font-size: 3.5rem;
            }
        }

        html,
        body {
            height: 100%;
        }

        body {
            display: -ms-flexbox;
            display: flex;
            -ms-flex-align: center;
            align-items: center;
            padding-top: 40px;
            padding-bottom: 40px;
            background-color: #f5f5f5;
        }

        .form-signin {
            width: 100%;
            max-width: 330px;
            padding: 15px;
            margin: auto;
        }
        .form-signin .checkbox {
            font-weight: 400;
        }
        .form-signin .form-control {
            position: relative;
            box-sizing: border-box;
            height: auto;
            padding: 10px;
            font-size: 16px;
        }
        .form-signin .form-control:focus {
            z-index: 2;
        }
        .form-signin input[type="email"] {
            margin-bottom: -1px;
            border-bottom-right-radius: 0;
            border-bottom-left-radius: 0;
        }
        .form-signin input[type="password"] {
            margin-bottom: 10px;
            border-top-left-radius: 0;
            border-top-right-radius: 0;
        }
    </style>
</head>
<body class="text-center">
    <%= form_for(@user, {html: {class: "form-signin"}, url: {controller: 'user', action: 'login'}, method: "post"}) do |form| %>
        <i class="far fa-user-circle fa-8x mb-3"></i>
        <h1 class="h3 mb-3 font-weight-normal">ログイン</h1>
        <% if @error_message %>
        <p class="text-danger"><%= @error_message %></p>
        <% end %>
        <%= form.label :email, "メールアドレス", {class: "sr-only"} %>
        <%= form.email_field :email, class: "form-control", placeholder: "メールアドレス", required: true, autofocus: true, value: @email %>
        <%= form.label :password, "パスワード", {class: "sr-only"} %>
        <%= form.password_field :password, class: "form-control", placeholder: "パスワード", required: true, value: @password %>
        <%= form.submit "ログイン", class: ["btn", "btn-lg", "btn-primary", "btn-block", "mt-2"] %>
        <p class="mt-5 mb-3 text-muted">&copy; 2020 Diaries.</p>
    <% end %>
</body>
</html>
